# #FreeClimb API
#
# FreeClimb is a cloud-based application programming interface (API) that puts the power of the Vail platform in your hands. FreeClimb simplifies the process of creating applications that can use a full range of telephony features without requiring specialized or on-site telephony equipment. Using the FreeClimb REST API to write applications is easy! You have the option to use the language of your choice or hit the API directly. Your application can execute a command by issuing a RESTful request to the FreeClimb API. The base URL to send HTTP requests to the FreeClimb REST API is: /apiserver. FreeClimb authenticates and processes your request.
#
# The version of the OpenAPI document: 1.0.0
# Contact: support@freeclimb.com
# Generated by: https://openapi-generator.tech
# OpenAPI Generator version: 7.9.0
#

require "date"
require "time"

module Freeclimb
  # The parameters to use for the TTS. The complete list of valid values for the parameters attribute is shown below.
  class SayElevenLabsEngineParameters
    # The ID of the ElevenLabs model to use for the TTS. The complete list of valid values for the model_id attribute is shown below.
    attr_accessor :model_id

    # The ID of the ElevenLabs voice to use for the TTS. The complete list of valid values for the voice_id attribute is shown below.
    attr_accessor :voice_id

    # The language code to use for the TTS. The complete list of valid values for the language_code attribute is shown below.
    attr_accessor :language_code

    attr_accessor :voice_settings

    attr_accessor :pronunciation_dictionary

    attr_accessor :seed

    attr_accessor :previous_text

    attr_accessor :next_text

    attr_accessor :previous_request_ids

    attr_accessor :next_request_ids

    attr_accessor :apply_language_text_normalization

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        model_id: :model_id,
        voice_id: :voice_id,
        language_code: :language_code,
        voice_settings: :voice_settings,
        pronunciation_dictionary: :pronunciation_dictionary,
        seed: :seed,
        previous_text: :previous_text,
        next_text: :next_text,
        previous_request_ids: :previous_request_ids,
        next_request_ids: :next_request_ids,
        apply_language_text_normalization: :apply_language_text_normalization
      }
    end

    # Returns all the JSON keys this model knows about
    def self.acceptable_attributes
      attribute_map.values
    end

    # Attribute type mapping.
    def self.openapi_types
      {
        model_id: :String,
        voice_id: :String,
        language_code: :String,
        voice_settings: :SayElevenLabsEngineParametersVoiceSettings,
        pronunciation_dictionary: :"Array<SayElevenLabsEngineParametersPronunciationDictionaryInner>",
        seed: :Integer,
        previous_text: :String,
        next_text: :String,
        previous_request_ids: :"Array<String>",
        next_request_ids: :"Array<String>",
        apply_language_text_normalization: :Boolean
      }
    end

    # List of attributes with nullable: true
    def self.openapi_nullable
      Set.new([])
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      if !attributes.is_a?(Hash)
        fail ArgumentError, "The input argument (attributes) must be a hash in `Freeclimb::SayElevenLabsEngineParameters` initialize method"
      end

      # check to see if the attribute exists and convert string to symbol for hash key
      attributes = attributes.each_with_object({}) { |(k, v), h|
        if !self.class.attribute_map.key?(k.to_sym)
          fail ArgumentError, "`#{k}` is not a valid attribute in `Freeclimb::SayElevenLabsEngineParameters`. Please check the name to make sure it's valid. List of attributes: " + self.class.attribute_map.keys.inspect
        end
        h[k.to_sym] = v
      }

      self.model_id = if attributes.key?(:model_id)
        attributes[:model_id]
      else
        "eleven_turbo_v2_5"
      end

      if attributes.key?(:voice_id)
        self.voice_id = attributes[:voice_id]
      end

      self.language_code = if attributes.key?(:language_code)
        attributes[:language_code]
      else
        "en"
      end

      if attributes.key?(:voice_settings)
        self.voice_settings = attributes[:voice_settings]
      end

      if attributes.key?(:pronunciation_dictionary)
        if (value = attributes[:pronunciation_dictionary]).is_a?(Array)
          self.pronunciation_dictionary = value
        end
      end

      if attributes.key?(:seed)
        self.seed = attributes[:seed]
      end

      if attributes.key?(:previous_text)
        self.previous_text = attributes[:previous_text]
      end

      if attributes.key?(:next_text)
        self.next_text = attributes[:next_text]
      end

      if attributes.key?(:previous_request_ids)
        if (value = attributes[:previous_request_ids]).is_a?(Array)
          self.previous_request_ids = value
        end
      end

      if attributes.key?(:next_request_ids)
        if (value = attributes[:next_request_ids]).is_a?(Array)
          self.next_request_ids = value
        end
      end

      if attributes.key?(:apply_language_text_normalization)
        self.apply_language_text_normalization = attributes[:apply_language_text_normalization]
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      []
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      if @model_id.nil?
        false
      else
        list_invalid_properties.length == 0
      end
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(other)
      return true if equal?(other)
      self.class == other.class &&
        model_id == other.model_id &&
        voice_id == other.voice_id &&
        language_code == other.language_code &&
        voice_settings == other.voice_settings &&
        pronunciation_dictionary == other.pronunciation_dictionary &&
        seed == other.seed &&
        previous_text == other.previous_text &&
        next_text == other.next_text &&
        previous_request_ids == other.previous_request_ids &&
        next_request_ids == other.next_request_ids &&
        apply_language_text_normalization == other.apply_language_text_normalization
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(other)
      self == other
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [model_id, voice_id, language_code, voice_settings, pronunciation_dictionary, seed, previous_text, next_text, previous_request_ids, next_request_ids, apply_language_text_normalization].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      new.build_from_hash(attributes)
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      self.class.openapi_types.each_pair do |key, type|
        if attributes[self.class.attribute_map[key]].nil? && self.class.openapi_nullable.include?(key)
          send(:"#{key}=", nil)
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[self.class.attribute_map[key]].is_a?(Array)
            send(:"#{key}=", attributes[self.class.attribute_map[key]].map { |v| _deserialize($1, v) })
          end
        elsif !attributes[self.class.attribute_map[key]].nil?
          send(:"#{key}=", _deserialize(type, attributes[self.class.attribute_map[key]]))
        end
      end

      self
    end

    # Deserializes the data based on type
    # @param string type Data type
    # @param string value Value to be deserialized
    # @return [Object] Deserialized data
    def _deserialize(type, value)
      case type.to_sym
      when :Time
        Time.parse(value)
      when :Date
        Date.parse(value)
      when :String
        value.to_s
      when :Integer
        value.to_i
      when :Float
        value.to_f
      when :Boolean
        if value.to_s =~ /\A(true|t|yes|y|1)\z/i
          true
        else
          false
        end
      when :Object
        # generic object (usually a Hash), return directly
        value
      when /\AArray<(?<inner_type>.+)>\z/
        inner_type = Regexp.last_match[:inner_type]
        value.map { |v| _deserialize(inner_type, v) }
      when /\AHash<(?<k_type>.+?), (?<v_type>.+)>\z/
        k_type = Regexp.last_match[:k_type]
        v_type = Regexp.last_match[:v_type]
        {}.tap do |hash|
          value.each do |k, v|
            hash[_deserialize(k_type, k)] = _deserialize(v_type, v)
          end
        end
      else # model
        # models (e.g. Pet) or oneOf
        klass = Freeclimb.const_get(type)
        klass.respond_to?(:openapi_one_of) ? klass.build(value) : klass.build_from_hash(value)
      end
    end

    # Returns the string representation of the object
    # @return [String] String presentation of the object
    def to_s
      to_hash.to_s
    end

    # to_body is an alias to to_hash (backward compatibility)
    # @return [Hash] Returns the object in the form of hash
    def to_body
      to_hash
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

    # Outputs non-array value in the form of hash
    # For object, use to_hash. Otherwise, just return the value
    # @param [Object] value Any valid value
    # @return [Hash] Returns the value in the form of hash
    def _to_hash(value)
      if value.is_a?(Array)
        value.compact.map { |v| _to_hash(v) }
      elsif value.is_a?(Hash)
        {}.tap do |hash|
          value.each { |k, v| hash[k] = _to_hash(v) }
        end
      elsif value.respond_to? :to_hash
        value.to_hash
      else
        value
      end
    end
  end
end
